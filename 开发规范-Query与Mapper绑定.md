# 开发规范：Query 与 Mapper 绑定

**适用范围**：所有 MyBatis Query 对象、ReadDAO/WriteDAO 接口、Mapper XML 及其调用方。

制定依据不是编码偏好，是两起数据库 OOM 事故的复盘：**查询条件没有真正生效，
SQL 静默退化成大范围扫描**。这类问题跨 Java 与 XML 两侧，不报错、不抛异常、
测试也未必失败，只有在数据量涨上来之后才炸。

每条规范都标注了违反后果、能否自动检查。**能自动检查的已接入
HJLY 开发规范检查 插件**（`Analyze | 扫描 Query/Mapper 绑定问题`）。

---

## 一、编辑完整性：改一处必须改全

这是最容易产生 bug 的一类，因为编译器不会报错。

### 规范 1.1　删除 Mapper 里的查询条件时，必须同步处理 Query 字段与调用方

删掉 `<if test="query.xxx">...</if>` 之后，必须做完下面三件事之一：

- 一并删掉 Query 类里的 `xxx` 字段，并清理所有调用方的 `setXxx(...)`
- 确认该字段仍被其他语句使用，保留
- 若暂时保留但无人使用，在字段上加注释说明用途与时限

**违反后果**：调用方以为自己在按这个条件过滤，实际这个值传下去无人使用，
结果集不受限。**这正是 OOM 事故的形态**。

**自动检查**：`DAL-010`（原 `D1`）—— 调用方 set 了某字段，但该 Query 类关联的**所有**语句都不引用它。

> 与 `A` 类的区别：`A` 看单条语句，一个 Query 被多条语句复用、各自只用一部分字段是正常的；
> `DAL-010` 是该字段在所有语句里都不存在，却还有人 set —— 那就不是"这条语句不需要"，是残留。

### 规范 1.2　重命名 Query 字段时，必须同步改 Mapper

改了 `private Long xxxId;` 的名字，必须同步改 XML 里的 `<if test="query.xxxId">` 与 `#{query.xxxId}`。

**违反后果**：Java 侧有编译器兜底，**XML 侧没有**——OGNL 表达式里的字段名写错只会
静默求值失败，`<if>` 永不命中，条件消失。

**自动检查**：`DAL-002`（原 `B1`/`B2`；能找到近似字段多为改名漏改，找不到则单列）。

### 规范 1.3　新增 Query 字段后，必须在本次提交内被使用

加了字段就要有语句引用它，否则删掉。

**违反后果**：死字段积累会让后来者误以为"这个条件是生效的"。

**自动检查**：`DAL-011`（原 `D2`）—— 字段既无人 set 也无语句引用。风险低，属清理项。

---

## 二、命名一致性：跨层不得改名

### 规范 2.1　跨层转换禁止改字段名

```java
// 禁止
stockDetailQuery.setStockDateDetailId(webQuery.getStockDataDetailId());   // Date / Data
scenicQuery.setDataStatuses(webQuery.getStatus());                        // 单数 / 复数
```

同一业务概念在各层必须同名。确需转换语义（如单值转集合、枚举转换）的，
在方法上注释说明理由。

**违反后果**：现在不报错，是因为两边碰巧一致。任何一侧被人"顺手改对"，
条件立刻静默失效。实测全库有 6 处编辑距离≤2 的改名（`customId`/`customerId`、
`protocolCateGory`/`protocolCategory`、`datasetLabeIds`/`datasetLabelIds` 等），
全是打字错误固化下来的。

**自动检查**：`DAL-030`（原 `N1`/`N2`；高度相似疑似手滑 / 单复数集合变体）。

### 规范 2.2　禁止跨模块同名 Query 类

**违反后果**：工具按简单类名索引时会互相污染。实测 `StockDetailWebQuery` 有 3 个、
`ShopSettleWayWebQuery` 有 2 个，直接导致过一次误判——一个模块的安全判定被另一个模块的
同名类带偏。人读代码时同样会认错。

**自动检查**：`DAL-021`（原 `S2`）。

### 规范 2.3　`@Param` 统一命名为 `query`

DAO 方法上的 Query 参数一律 `@Param("query")`，与 XML 里的 `query.xxx` 对齐。

**违反后果**：XML 里的前缀取决于 `@Param` 的值（没有注解则取形参名），
形参名五花八门会让人和工具都容易对错基准。开发这个插件时就因为取错参数名虚报过 32 条。

**自动检查**：`DAL-022`（原 `S3`）。

---

## 三、可审计性：不要让代码变成黑盒

### 规范 3.1　`transform` 方法内禁用 `BeanUtils.copyProperties`

Query 对象的转换方法里，字段赋值一律写成显式 `set`/`get`。

**违反后果**：反射拷贝在运行期才决定拷哪些字段，**任何静态分析都看不见**——
正则看不见，AST 看不见。实测全库 58.8% 的 transform 方法含它，
直接导致字段流向追不动，大量调用链降级成"未产出结论"。

这条不是编码洁癖，是**其他所有检查能否工作的前提**。实测案例：

```java
// hjly-dataset：DatasetGroupWebQuery.transform
DatasetGroupQuery q = BeanUtils.copyProperties(webQuery, DatasetGroupQuery.class);
```

`groupNameLike` 经反射拷到了 DAL 层 Query，而 Mapper 里只有 `group_name = #{query.groupName}`
精确匹配、没有 like 条件——这是个真问题（人工复核已确认）。但因为赋值走的是反射，
扫描器看不到"谁 set 了这个字段"，`DAL-010`（改一半的残留）就退化成了 `DAL-011`（死字段），
归类直接错位。**违反这一条，不只是自己难查，还会让别的检查一起失效。**

**自动检查**：`DAL-020`（原 `S1`）。

### 规范 3.2　`<include refid>` 只用字面量

不要用 `${}` 拼接 refid。

**违反后果**：动态 refid 在运行期才确定，展开不了，该段 SQL 对分析工具直接消失。

**自动检查**：已登记为覆盖缺口（当前全库实测为 0，未触发）。

---

## 四、API 使用

### 规范 4.1　`BeanUtils.copyProperties` 必须确认参数顺序

项目里混用了两个**参数顺序完全相反**的同名方法：

```java
com.hjly.commontool.util.core.BeanUtils.copyProperties(dest, orig)   // 目标在前
org.springframework.beans.BeanUtils.copyProperties(source, target)   // 源在前
```

新代码统一使用其中一种，并在类的 import 处保持一致。

**违反后果**：写反时，刚 `new` 出来的空对象被当作拷贝源，把目标对象整个清空。
若目标是 Query，查询条件成片失效；若目标是 DO 并被 save，是数据写入丢失。

**自动检查**：`DAL-004`（原 `CP`）—— 按 `resolveMethod()` 拿到的真实声明类判定，不靠 import 猜。

### 规范 4.2　标准模板语句不得手改条件与绑定的对应关系

`listByQuery` / `listPageByQuery` / `getByQuery` / `save` / `update` 这些由模板生成的语句里，
`<if test="a">` 判断哪个字段，块内就绑定哪个字段。

**违反后果**：`<if test="couponBatchId != null"> coupon_batch_id = #{gmtCreate}</if>`
这种写法会把创建时间写进券批次 ID 列——不是查询退化，是**写错数据**。

定制语句里"判断 a 用 b"常是真实业务需求，不在此列。

**自动检查**：`DAL-003`（原 `C`）—— 只查标准模板 id。

---

## 五、规范与检查项对照

| 规范 | 检查项（规则编号） | 实时高亮 | 严重度 | 状态 |
|---|---|---|---|---|
| 1.1 删条件要清理字段与调用方 | `DAL-010`（原 `D1`） | —（GLOBAL） | Critical | ✅ |
| 1.2 改字段名要同步改 XML | `DAL-002`（原 `B1`/`B2`） | ✅ | Critical | ✅ |
| 1.3 新增字段必须被使用 | `DAL-011`（原 `D2`） | —（GLOBAL） | Minor | ✅ |
| 2.1 跨层禁止改名 | `DAL-030`（原 `N1`/`N2`） | ✅ | Major | ✅ |
| 2.2 禁止跨模块同名 Query 类 | `DAL-021`（原 `S2`） | —（GLOBAL） | Major | ✅ |
| 2.3 `@Param` 统一为 `query` | `DAL-022`（原 `S3`） | ✅ | Minor | ✅ |
| 3.1 transform 内禁用反射拷贝 | `DAL-020`（原 `S1`） | ✅ | Major | ✅ |
| 3.2 refid 只用字面量 | 覆盖缺口登记 | — | — | ✅ |
| 4.1 copyProperties 参数顺序 | `DAL-004`（原 `CP`） | ✅ | Blocker | ✅ |
| 4.2 标准模板不得改对应关系 | `DAL-003`（原 `C`） | ✅ | Blocker | ✅ |
| （非规范）条件失效 | `DAL-001`（原 `A`） | —（GLOBAL） | Blocker | ✅ |

编号规则：`<领域>-<序号>`，全局唯一，进报告与豁免文件。豁免文件里写旧的变体码
（`B1`）或新的规则编号（`DAL-002`）都能命中；`DAL-004`/`DAL-022`/`DAL-002`/
`DAL-030` 附带一键修复（只做能改完整的）。

---

## 六、工具本身的准则：漏报比误报危险

这条约束的是扫描器，不是业务代码，但它决定了上面所有规范能不能真正落地。

**误报是显性的，漏报是静默的。** 误报有人看见、确认一次、标记豁免，成本一次性；
漏报没有任何人会发现它，风险一直留着。所以判定逻辑遇到拿不准的情况，
**必须报出来并标注不确定**，不能压掉。

要区分两种处理，只有前者可以接受：

| 做法 | 判定 |
|---|---|
| **降级为覆盖缺口**——不下结论，但显式登记"这里没看到" | ✅ 人能看见，可追 |
| **静默压制**——既不报也不登记，结论凭空消失 | ❌ 这就是漏报 |

开发中犯过一次后者：反射拷贝看不见拷了哪些字段，当时为了避免 `D2` 误报，
把目标 Query 的字段全塞进"可能被 set"，结论就此消失。已改为照常报出，
并在消息里标注"该 Query 经 copyProperties 反射赋值，字段来源无法静态确认，需人工核实"。

配套的是**团队共享的豁免机制**：人工确认某处是误报后记入
`<模块仓库>/.binding-scan-ignore.yml`，随代码进版本库。
`git pull` 同步、`git blame` 溯源，一个人确认一次，全团队都不必重走。

豁免条目必须带 `reason` / `by` / `at`，且**被豁免的条目仍要在汇总里可见**——
它是"已确认的记录"，不是"删掉"。看到某处被豁免的人应当能立刻知道是谁、基于什么理由确认的，
不同意时可以重新讨论，而不是面对一个凭空消失的结论。

## 七、怎么用

**提交前**：`Analyze | 扫描 Query/Mapper 绑定问题`，导出 CSV 自查。

**CI**：`./gradlew runIde -PscanProject=<项目> -PscanOut=<CSV>`。

**结果定性**：所有结果都是 `CANDIDATE`——排查线索，不是确认的缺陷单。
汇总里会同时列出**覆盖缺口**（扫描未能产出结论的部分），
这部分不等于没问题，只是没看见。只报发现不报缺口会让人误以为扫过就等于安全。
